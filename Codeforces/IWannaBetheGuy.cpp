#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
#define fastio() ios_base::sync_with_stdio(false);cin.tie(NULL);cout.tie(NULL)
#define rep(i,n) for(int i=0;i<n;i++)

int main() {
    fastio();
 	int n; cin >> n;
 	vector<int>a(n);
    
    int m;
    cin >> m;
    for(int i=0;i<m;i++){
        cin >> a[i];
    }
    int k;
    cin >> k;
    for(int j=0 ; j<k;j++){
        cin >> a[j];
    }
//USE SET NEXT TIME U FUKING DONUT U DUMBASS TRYNA LEARN DP BEFORE SETS SHTUPID
    

}